[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: все пути отдаём index.html, React Router сам разберётся
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
